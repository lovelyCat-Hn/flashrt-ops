---
name: galbot-pi05-g1-finetune-data
description: G1 真机数据集 pick_place_balence 判读 + 16 维部署全链路已搭好（prep/align/run 三件套，2026-09-22 真机验证）；动作语义=增量；数据集回放开环验证管线已就位
metadata: 
  node_type: memory
  type: project
  originSessionId: 24ded4cc-d86c-4589-8234-2f1634018dc7
  modified: 2026-09-24T05:48:24.955Z
---

**G1 真机数据集判读（2026-09-21，echo 机 `~/datasets/pick_place_balence/`，zip 857M 解压 870M）**

- **格式**：lerobot **v3.0** 标准格式（meta/info+stats+tasks+episodes、data/chunk-000/*.parquet、videos/ 外挂 mp4），robot_type=`galbot_one_golf`，**30fps，199 条轨迹 / 97,149 帧**，三相机 `head_right / left_arm / right_arm` 均已 224×224。任务指令英文双臂句式（task_index 0/1）。meta/stats.json 含全套 min/max/mean/std/q01..q99。
- **动作/状态语义（2026-09-22 二次修正）**：`action` **16 维** = [**右臂7(rad) + 右夹爪(0~100%) + 左臂7 + 左夹爪(0~100%)**]；`observation.state` **23 维** = 上述 16 维 + **leg_joint1-5 + head_joint1-2**。**块序【右臂在前】——meta/info.json 的 features names 是唯一权威**。曾误记"左臂在前"并按它装配 state/下发目标：右臂统计喂左臂读数，预热把机械臂甩到背后（用户"到背面来抓东西了"）；离线复算旧错序恰好复现真机首跑"[-1,1] 外 15 维"。腿/头直读始终正确（episode0 第0帧 leg 0.580/1.433/0.902/0/0 + head −0.061/0.330 与 SDK 实测全等）。episode0 第0帧=标准工作位（各轨迹同一起点位，与 state.mean 几乎重合——"均值幻影"假设已排除）。数据集现已在本机 `~/holy/datasets/pick_place_balence/`（870M，带三相机 mp4）。SDK `get_joint_names()` 恰 23 个关节一一对应。
- **关键结论：与 pi05_libero 的 7 维"对应不上，也不该对应"**——正确路线=微调 16 维 G1 版 pi05（x86 GPU，lerobot 管线，prompt 沿用数据集英文句式）。
- **动作语义 = 增量（2026-09-24 用户确认，判读基石）**：模型/数据集 action 输出是 **Δ（关节 rad 增量 + 夹爪 % 增量）**，不是绝对目标——帧 0 处 action 与 state 逐位相等（Δ=0）、保持段输出≈0 是正确行为。判读动作合理性必须对 Δ=action[t]−state[t] 比，对绝对 action 算 cos 会全崩且会误诊成 OOD。数据集两 task（pick/place 句式，task_index 0/1）**初始位姿不同**，跨 task 混着判读位姿会踩坑。
- **权威权重路径（2026-09-24 用户指定）**：`~/holy/models/pi05_g1_pretrain/pi05_g1_040000/pretrained_model/`（lerobot 布局：config.json + model.safetensors + **policy_preprocessor_step_3_normalizer_processor.safetensors = 权威统计**，FlashRT 加载器在目录无 norm_stats.json 时自动扫该 safetensors 对）。`pi05_g1_deploy/` 只是包装目录（软链同一 safetensors + 手工 norm_stats.json）；`compare_norm_stats.py` 三方核对实证两者逐维一致（0/23 不相交、action q01/q99 差 0）。
- **数据集回放开环验证管线（2026-09-24 打通并验证）**：`scripts/inference/extract_dataset_frames.py`（parquet→npz，带 dataset_root/每相机视频寻址/自检锚点，flash_pyrt311 直跑）→ `run_dataset_replay.py`（mp4 帧+state 走与真机同一条模型链，cosΔ(0/1 对齐)+落域+保持帧标注，--dump 逐维，--check-graph 计数图重放）。bf16 实测（ep0 每秒 1 帧×8）：运动帧 cosΔ 最高 1.0（夹爪开合事件时刻准、幅度过冲 ~2×），落域 2%，graph 重放生效，延迟 ~350ms。**局限**：开环单帧固定噪声会随机错过事件边界（如闭合 Δ−33.5 未触发），属探针固有，不是链路 bug；真值 KPI 看闭环。

**16 维部署全链路已搭好并真机验证（2026-09-22）**——pi0.5 原生动作隐空间 32 维（action_out_proj [32,1024]），16/7 维只是消费侧切片，权重零改动。三件套（`~/holy/scripts/`）：
1. `inference/g1_ckpt_prep.py`：微调输出 → 部署目录（软链 14G + config + norm_stats.json 带 `norm_mode` 标记 + flashrt_deploy.json 部署清单）。预检：safetensors header 张量校验（键名带 `.weight` 后缀！）、config/stats 维数三方一致、tokenizer 在位。
2. `eval/norm_align_check.py`：Part A 双语义数学往返（1e-5）→ Part B state 落域+常量窄维预警 → Part C **单实例 stats 翻转法**实证引擎分发（反解 raw 一致 6e-08、mean_std 仿射符合 8e-09）→ Part D 落域。
3. `inference/run_g1_inference.py`：真机只读推理。manifest 自动配置零参数；显式名字读 23 维关节 → `normalize_state` → predict → (10,16) + 三层打点（ctrl-hz 默认 30=数据集 fps）。实测（源机，服务常驻负载）：①纯推理 232ms（≈基线）、②取图 22ms、③端到端 min 259/p50 560/max 853ms。**③层大方差已破案（2026-09-23）**：state 以十进制文本拼进 prompt（`format_pi05_prompt`），关节值漂→bin 数位变→token 数变；`state_prompt_mode` 默认 **exact**=每种长度一条 pipeline，换长即整条重建+重 autotune（~800ms；合成实验实锤 A→B 793ms、切回 A 511ms）。修复=`FLASHRT_PI05_STATE_PROMPT_MODE=fixed`（定长 200 一条 pipeline 只换 embeds），实测含 state 来回切全部 240-253ms 恒定；run 两脚本已内置。state 静止时 exact 也稳定——此前"偶发慢"都发生在关节运动之后。**坑：`os._exit` 不刷新 stdout 缓冲**——重定向到文件时打印全丢（终端行缓冲不受影响），重定向跑批关键 print 要 `flush=True`。**推理→SDK→关节闭环已打通（2026-09-22）**：`g1_pose_warmup.py` 预热到工作位（臂 16 维归一化全部入域，余 3 维 leg 窄维噪声）→ `run_g1_execute.py --exec` 3 步全 SUCCESS、回读最大偏差 2.3 mrad。工具已入库（483de44）。

**FlashRT 本地补丁（未推上游）**：`6425fe8d` action_dim 参数+三处切片可配置；`bff59419` `unnormalize_actions` 按 norm_stats 顶层 `"norm_mode"` 分发（`q01_q99` 默认旧行为逐位不变 / `mean_std` = x*std+mean）+ 新增 `normalize_state`（flash_rt/core/utils/actions.py）。**关键事实：FlashRT 不归一化 state**——`discretize_pi05_state` 直接把输入按 [-1,1] 分 256 bin 进 prompt，喂原始关节值=静默打满 bin，必须调用方先 `normalize_state`。**教训：monkeypatch 前端 `__init__` 必须 `functools.wraps`，否则 api.py 按签名转发静默丢参**。**坑：跨两次模型实例同噪声 raw 不逐位一致**（14G 常驻改变显存布局/cuBLAS 路径），对齐实验须同实例内做（stats 翻转法）。

**归一化语义（最隐蔽坑）**：norm_mode 选错=动作系统性畸变。lerobot 管线默认 MEAN_STD→`mean_std`；openpi 默认分位→`q01_q99`。数据集窄维 7 个（leg/head，分位宽 <1e-3）：部署摆位须与采集一致否则 bin 打满；run 脚本已内置越界维提示。**夹爪标定已落地（2026-09-24）**：SDK 宽度米 0.0005/0.1200（2026-09-23 实测）已写进 pi05_g1_deploy/flashrt_deploy.json 的 gripper 块——models 不入库，**重跑 g1_ckpt_prep 必须带 `--grip-wmin 0.0005 --grip-wmax 0.1200` 否则标定又变 null**。**config [run].ckpt 坑（2026-09-24 修）**：g1.toml 曾写计划名 pi05_g1_ft（本机从未存在）致 warmup 首行就退出，已改指 pi05_g1_deploy；BUILTIN 保留 pi05_g1_ft 因其他设备可能真有该目录（用户口径）；right_arm_recover.py 仍硬编码 pi05_g1_ft 无覆盖。
- 系统 python3.8 有 pandas+pyarrow；**2026-09-24 用户把 pandas 2.1.4 + pyarrow 16.1.0 也装进了 flash_pyrt311**（numpy==1.26.4 幸存未被动，红线组合仍成立）——环境自此与打包 bundle 有分化：重打 FlashRT 环境包时要么带上这两个包、要么保留 npz 两段式兼容；extract_dataset_frames 现在环境 python 直跑，一阶段即可。

**⚠ 闭环旧案翻案线索（2026-09-24）**："模型漂移持续同号 ~95 mrad/指令"当时归因场景不匹配——真因很可能是 **run_g1_loop 把 chunk 当绝对目标执行**（plan_step: arm_tgt−cur），与模型增量输出错位 → 每条指令恒向同向滑 delta_max。loop 尚未按增量语义修正（跑真机前必须改）【注：本线 echo 机已在 c3702c6 修正 execute/loop 增量语义，此处为另一设备线的历史记录】；同坑：send_grip 把增量 % 当绝对 0~100% 下发，负增量会被 clip 成 0% = 砸紧。**正确增量执行范式见 `run_dataset_execute.py`**（数据集图像→真机执行：图像走 mp4 帧时间线、state 用真机读数、目标=当前+clip(Δ合步和)；--align 先对齐 episode 帧 0 录制位姿——warmup 目标是 state.mean 与帧 0 可差 ~0.8 rad——护栏基准随之重置）。**推理固定噪声仍要做**（set_prompt+infer+manual_seed(seed+帧号)，消掉每轮抽签），**坑：进程内第一次模型调用必须走 predict 建管线**，直接 set_prompt+infer 炸 `_graph_torch_stream` AttributeError——先 predict 一次再走 infer。
**⚠ 抬臂真相（2026-09-24 二次破案，推翻"随机噪声野策略"旧结论）**：固定噪声后真机仍抬臂；离线闭环模拟（数据集图像+模拟 state 递进，`/tmp/sim_closed_loop.py` 已入库 `scripts/inference/sim_dataset_closed_loop.py`）**逐关节复现真机终态**（Lj7 −914 vs 真机 −964，终态都落 ep0 帧 64-66）→ 链路（图像/state/归一化/增量语义）全对，行为是模型本身的：①回放"帧0-21保持"是阈值假象（|输出|<0.1 盖住了 15-60 mrad/步的持续任务方向输出，真值 Δ 只有 ~5 mrad）→ **模型从不下"保持"，一直小步往任务推**；②小信号过冲 3-6×（训练 QUANTILES==部署 q01_q99 语义一致，非归一化 bug，pi0.5 固有增益）；③闭环 0.15 rad/轮预算恒向积累 10 轮 ≈0.9 rad = 抬臂，终态=数据集自己的抓取前伸展位姿（帧 66 图像左臂高举）。**判读协议**：数据集前 ~40 帧几乎是静场，别用"没动=对、动了=错"判真机；真机放场景位（桌面+蓝箱+A 物）跑长轮次看抓取才是 KPI；验证跟踪精度用 --start-frame 40+ 真运动段。
**夹爪标定完成（2026-09-23，部署机真机）**：`width_min=0.0005 m（0% 闭合）/ width_max=0.1200 m（100% 张开）`，双通道（`get_gripper_state().width` 与 `get_joint_positions([],[左/右]_gripper_joint1)`）逐位一致、端点经目视+端到端开合双确认，已写进 `pi05_g1_ft/flashrt_deploy.json`。**SDK 夹爪反馈滞后坑（实测实锤）**：`set_gripper_command` 立即执行，但状态流冷启动/滞后可达 **~6.3s**（期间 `is_moving=False`、width 恒旧值，预热读 5s 都盖不住），随后 10Hz 平滑追踪——判断「没动」前必须轮询 ≥8s，`gripper_calib.py` 已按此定型（活性守卫 8s/单程超时 30s）。标定工具 `scripts/inference/gripper_calib.py` 入库。控制接口：`set_gripper_command(G1JointGroup.left/right_gripper, width_m, velocity_mps, effort, is_blocking)`。

**执行侧夹爪下发已接入（2026-09-23，干跑验证）**：`run_g1_execute.py` / `run_g1_loop.py` 加 `--grip` 开关（默认关=原行为）——dim7/dim15（0~100%，越界截断）→ manifest 标定换算 `wmin + p/100*(wmax-wmin)` → `set_gripper_command`；变化 < `--grip-chg`（默认 2%）不重发；**非阻塞只发不等反馈**（6.3s 滞后坑，等待会拖死控制环），终态核对放结束后 sleep 8s 再读。合步/traj 模式取 chunk 末步夹爪维（与臂目标同刻语义）；`--grip-speed` 默认 0.05 m/s、`--grip-effort` 30 N。无标定的 ckpt 开 `--grip` 直接拒绝。两脚本干跑（含真机 SDK init+模型加载+规划打印）全绿，`--exec` 真执行待上机。腿/头维度仍然永不下发。

**state 侧夹爪单位 bug（2026-09-23 修复）**：`run_g1_execute/loop` 曾把 SDK 米直接喂 `normalize_state`（数据集 dim7/15 是 0~100%）——0.12m 被当 0.12% 算，模型恒看到「≈闭合」，夹爪物理全开也错。`run_g1_inference.py` 早有换算（read_state），execute/loop 补齐 `state_from_joints()`（clip 0~100）。**数据集实证：199 轨全部从夹爪 0%（闭合）起步**（episode 首帧 state[7]/[15] 全 0）→ `g1_pose_warmup.py` 加 `--grip`：闭合到 width_min 对齐训练起点，不闭合则首帧观测分布错位；闭合命令**并行化**——段①（--skip-zero 则段②）前一次非阻塞发出，与臂动作重叠掉 6.3s 反馈滞后（夹爪与臂位无物理耦合，无需分段等待，末端达标判定统一核对 dim7/15）；warmup 的 read23 也按标定换算 0~100% 展示/判达标。**躯干/腿已并入段②**（2026-09-23，用户提出）：腿 5 关节对齐 state.mean=[0.581,1.433,0.902,0,0]（=数据集工作高度，joint2 行程 ~1.43 rad 即站高抬升，限速 0.2 rad/s），`--skip-leg` 可退。**腿路径真机验证通过（2026-09-23 预热全绿）**：set_joint_positions 直接收腿 5 关节 SUCCESS 且逐关节到位，无需换 GalbotMotion 腿接口；映射无反转三证=meta/info.json dim16-20 顺序 + SDK 关节名一一对应 + episode0 帧0 腿值与 SDK 实测全等；行程澄清：state.mean 距自然站高（实测 leg_joint2≈1.2）仅 13-16°，「82°」是相对 SDK 零位的行程、机器人不停在零位（平衡控制器托站高）。**零位接口 TIMEOUT 坑（2026-09-23 真机）**：move_whole_body_joint_zero 的臂部分走【运动规划器+碰撞检查】（leg/head 才是直控），且**无臂速度/超时参数**（签名只有 leg_head_*；见 g1/__init__.pyi）——从伸直/大偏移姿态规划回零路径可能内部超时，臂停中途。恢复=重试（常为暂态）或 --skip-zero（段② set_joint_positions 关节直控不经规划器，注意清空臂前空间）；warmup 已加超时后交互式选择（回车重试/s 跳过）。**外力掰臂→驱动层故障（同日实测）**：控制器使能保持下手动掰臂 → 关节驱动层位置误差/过流保护，新指令全拒；特征=读数通道正常+get_active_controller 仍显示 right_arm_pvt_ctrl（RT 层无告警）。恢复=对故障臂单独 stop_controller→start_controller 断电重启清故障（⚠ stop 后臂失力变软要先扶住）→ 小步试动确认 → 回工作位；脚本 scripts/inference/right_arm_recover.py（发运动命令的恢复脚本由用户在机器旁执行）。修复后模型输出随真实夹爪状态变化（干跑 A/B 实证：state 修正前后夹爪维输出不同）。

**pi05_g1_ft 首次带权重真机 smoke（2026-09-23，echo 机）**：执行链路全绿（3 步 SUCCESS、回读偏差 1.9 mrad、夹爪指令→终态 13.1→13.0/19.9→20.0mm 首次完全对上，8s 终读消化 6.3s 滞后）。**action 语义钉死=绝对关节角**：action[t]≈state[t+1]（三条 episode k=1.000、残差 0.006-0.008 rad；delta/起点偏移假设均被否），执行侧绝对目标处理正确。⚠ 注意 lerobot v3.0 此数据集全部 episode 连在**一个 parquet**里（须按 episode_index 拆分再算轨迹统计，直接 diff/cumsum 会跨轨迹重置出错）。**模型输出曾全员退化为数据集均值**（臂 14 维 ≈0 rad=行程中点=均值，夹爪 ≈13%≈均值 10%）——OOD 场景标准行为：训练场景=桌面蓝色料盒+小玩偶（双臂从桌沿伸入），实况=空旷地板无桌面（对比法：抽数据集 videos/ 三相机帧 vs SDK 实时取图，构图旁证相机映射正确）。**场景复现前动作无任务语义属预期**；场景摆好后的合格信号=chunk 臂维呈 -1.2~1.6 结构化数值（≈episode 首帧动作）、夹爪维 ≈0% 且抓取前保持 0。
**摆位判据=腕部视角，头部构图不足为准（2026-09-23 桌前实测）**：机器人上桌后头部构图已接近训练，chunk 仍均值（臂 14 维 **97% 贴零**、|均值| 0.018、夹爪 6~10%）——对比腕部视角才见真因：训练起点**爪几乎压在盒沿正上方，蓝盒+对象占腕部视野下半大比例**；摆位偏远/侧偏时盒子缩到视野边缘（看空桌板+背景），模型判「爪下无盒」继续待机。抓取对象实锤=**白色空气开关（红扳把）**（此前误记"小玩偶"），蓝盒放在带脚轮小推车上。摆位迭代工具 `scripts/inference/g1_scene_check.py`（三相机取图 6×2s 重试 + 与 docs/scene_reference/ 参考帧并排拼图，**不加载模型十几秒/轮**，重启后相机服务慢的教训已内置）；参考帧（训练 episode0 起点 + 桌前反例）入库 docs/scene_reference/。⚠ 数据集 videos 目录结构：videos/observation.images.*/chunk-000/file-001.mp4（相机目录下还有一层 chunk-000，不是 videos/chunk-000/）。
**第二课：state 比场景更敏感——挪底盘后臂漂必须重预热（同日实证）**：scene_check 拼图显示腕部构图已与训练基本一致后，chunk 仍均值（贴零 89%）——查 23 维 state：挪机器人过程中双臂下垂/被碰，臂关节偏工作位 0.3~0.5 rad（右 j2 -0.397/j5 -0.312、左 j2 +0.538/j4 -0.256），**4 个臂维出 [-1,1] 域**；腿/头零漂、夹爪 0% 正常。**工作流规则：每次挪动底盘后必须重跑 warmup（臂小偏直接 --skip-zero 直控回位）再推理**，已写进 COMMANDS.md §1①。

**【2026-09-23 下午颠覆性排查】模型 I/O 语义与「均值退化」误读 + 权重不复现训练行为**：
- **训练语义 = delta（相对动作）**：checkpoint 的 `policy_preprocessor.json` 有 `relative_actions_processor(enabled=true, exclude_joints=["gripper"])`——臂 14 维训练目标是 **action−state 的 delta**，夹爪维（dim7/15）保持绝对 %；后处理器 `absolute_actions_processor` 逆变换（绝对=delta+当前 state）。训练动作统计 q01/q99 仅 ±0.3~0.6 rad（delta 量级，绝对角是 -1.5~2.2）。**部署执行侧必须补 delta→绝对换算**（target = 预测时 state + delta[k]，不是 cumsum）。此前「action=绝对角实锤」验的是 parquet 层（处理器转换之前），没到模型 I/O 层。
- **归一化统计逐位一致**：训练 normalizer（policy_preprocessor_step_3 / postprocessor_step_0 safetensors）的 state/action q01·q99 与部署 norm_stats.json 最大差 0.000000——归一化层无嫌疑。
- **bf16 档与 INT8 行为相同**（贴零 89~97%）——量化排除。张量体检干净（无 NaN/塌缩，action_out_proj 等统计正常）——权重结构健康。
- **teacher-forced 判决**：喂训练 episode0 第0帧与第300帧的原版输入（三相机帧+原版 state+原句 prompt），模型输出 delta 与数据集同帧 delta **cos=-0.104（不相关）、幅度大 6 倍（0.18 vs 0.03）、夹爪 76% vs 数据集 0%**——模型不复现自身训练行为。剩两嫌疑人：①微调没练成/训练时相机映射接错（症状吻合：视觉接地废→输出与观测无关的「典型 delta」）；②FlashRT 对该 checkpoint 的转换/执行有 bug。**判别法=训练机（x86）lerobot 原生栈直接推理同一 checkpoint 对比 episode0 delta**：lerobot 对→FlashRT 路径问题；lerobot 也乱→训练问题。
- **state.mean 是两个 task 起始位姿的平均**：task0（pick up A，98 条）起始=夹爪 0%+臂低+j7=-1.54；task1（places，101 条）起始=夹爪 33%+高位+j7≈0.09；两者平均=「不存在的姿势」→ **warmup 目标必须改为 task0 起始中位姿**（task0 组内波动 ±0.32 rad）。此前记忆「episode0 第0帧与 state.mean 几乎重合」是错误结论（臂维差达 0.83 rad）——本次已纠正。
- 排查顺序教训：语义层（处理器链）→ 统计层（逐位 diff）→ 结构层（张量体检）→ 行为层（teacher-forced），层层排除才定位到「行为不复现」。
- 系统 python3.8 有 pandas+pyarrow（读 lerobot parquet 用它）；flash_pyrt311 无 pandas（勿装，numpy 钉版红线）。

**闭环执行现状（2026-09-23，run_g1_loop 真机 5/5 轮全绿）**：推理 p50 255 / max 257ms 恒定（fixed 模式），合步 3 步/指令 400ms，推理全重叠零站桩，重规划 2.5Hz。参数组合：`--speed 0.25 --settle-frac 0.5 --steps-per-cmd 3 --switch-dist 0.06`。**护栏按任务重标（2026-09-23，真抓取前置）**：max_excursion 0.25→3.0 rad（199 轨合法抓取全程包络 min 0.85/p50 1.17/p95 2.46/max 2.785 rad，旧值 100% 误触——0.25 是语义测试期防均值漂移值）；rounds 10→60（一轮≈0.4s/0.15 rad 行程，10 轮抓取中途断）。config/g1.toml 与 BUILTIN 已同步。**残留待调**：指令切换间仍有可见抖动（switch-dist 40% 处转向已消大部分停-走，剩切换瞬间一次）——方向：更早转向/更小合步间隔/流式重定向试验，或等厂商轨迹接口。模型漂移持续同号（~95 mrad/指令）属场景不匹配，护栏兜底正常。A/B 实验脚本 `g1_state_prompt_mode_test.py`、探针 `g1_alloc_probe.py` 均已入库留证。

**配置文件化 + 命令速查（2026-09-23）**：参数从 CLI 沉淀到 `config/g1.toml`（TOML 带注释，py311 标准库 tomllib 零依赖），四脚本（execute/loop/inference/warmup）经 `scripts/inference/g1_config.py` 统一三态解析：**CLI 显式 > config > 内置默认（BUILTIN 同源兜底）**——config 支持的 add_argument 一律 default=None，apply() 回填。安全红线：`--exec`（真实运动）不进配置仅 CLI；布尔显式关用 `--no-grip` 旗标。`COMMANDS.md`（仓根）=日常命令速查（预热/推理/smoke/闭环/标定/权重接入三步/场景要求/合格信号）。单测覆盖：BUILTIN 覆盖全部 mapping 键、三态各路径、真实 toml 解析。

**部署机 G1 权重接入完成（2026-09-23）**：`pi05_g1_040000`（lerobot 布局 `pretrained_model/`，model.safetensors 9,354,050,752 字节 bf16，train_config `normalization_mapping=QUANTILES` → `--mode q01_q99` 显式指定）三步走完：lerobot_stats_extract（夹爪维 q01=-0.00 ⚠ 为分位端毛刺非异常，q99≈100 正常）→ `g1_ckpt_prep --out ~/holy/models/pi05_g1_ft`（813 张量、32 维隐空间未动、16/23 维三方一致）→ `norm_align_check` 全绿（语义分发/同噪声确定性/落域 0% 越界；窄维 7 个=leg/head 属预期）。数据集同日到位 `~/holy/datasets/pick_place_balence`（199 轨/97,149 帧，meta names 右臂在前已核）。SDK 运行库 `/data/galbot/lib` 在位。**剩余**：真机 `run_g1_inference --ckpt pi05_g1_ft`（manifest 零参数）+ 夹爪标定（manifest width_min/max 仍 null，运行时透传并警告，拿到 SDK 满/零开度宽度后 prep 重跑带上 `--grip-wmin/--grip-wmax`）。

**【2026-09-23 晚最终定案】根因=INT8 量化，前两条结论翻案**：用户与同事核对确认模型输出=delta 语义；且"直接喂训练集数据推理、输出仍偏 0"。离线 teacher-forced 三档复测（新工具 `scripts/eval/tf_matrix.py`：训练集 ep0 五帧、干净 prompt、模型 delta vs 数据集 delta、单步+块均两口径+消融矩阵，纯离线不碰机器人）：
- 全 INT8：块均 cos **0.15**、夹爪 8~21% 垃圾值（数据集 0%）❌
- 仅编码器 INT8：块均 cos **0.27**、夹爪 5~19% ❌——**编码器 INT8 同样有毒**（FlashRT 注释只验过 encoder cosine，从没验动作端到端）
- 全 bf16：**块均 cos 0.98**（单步 0.90）、夹爪 0.2~0.4% ✅——**微调权重几乎完美复现训练行为：训练无问题、FlashRT 转换无问题**
- **翻案 1**："bf16≈INT8 行为相同、量化排除"——那次 A/B 在 OOD 真机场景比的，两档都输出均值动作、看不出差别。作废。
- **翻案 2**："teacher-forced cos=-0.104、模型不复现训练行为"——旧探针带括号 prompt（消融实测带括号显著扰动输出，cos 到基线仅 0.26~0.51）+ INT8 档，双重污染。干净 prompt+bf16 后判决反转。训练机 native 栈判别不再必需。
- 部署定档改 **bf16**：config/g1.toml `[inference].tier=bf16`，execute/loop 显式 `FVK_PI05_RTX_FORCE_BF16=1`（Orin 无 FP8 本就默认 bf16，显式防变）；延迟 ~255→~350 ms（闭环流水线可吸收）；BENCHMARKS.md 附录记录教训：**量化定档必须带动作质量验收（teacher-forced），不能只看延迟**。
- 连带修正：此前"场景摆位苛刻/腕部构图决定性/贴零=场景不对"等归因都是在 INT8 噪声输出上做的，判据作废；上真机用 bf16 重新校准（摆位对齐训练本身仍要做）。
- 同日落地的执行侧修正：warmup 目标切 task0 起始中位（`episode_start_task0.json`，state.mean=双 task 平均不可用）；execute/loop 补 delta→绝对换算（基准=预测时刻 state 臂位、随块走、续航路径沿用本块基准）；warmup q 键监听死代码修复（continue 后不可达，q 退出此前实际失效）。
- 环境：系统 python3.8 有 pandas+pyarrow（读 parquet 用它），flash_pyrt311 无；tf_matrix 用子进程自举把 episode 抽成 npz 缓存，flash 环境零新依赖。数据集视频=每相机 2 个共享 mp4，episode 按时间戳分段（帧 t → from_timestamp + t/30）。

**【2026-09-23 晚·数据集回放定级】task0 全绿可上真机，task1 夹爪未学会（训练侧）**：回放工具 `scripts/eval/tf_rollout.py`（bf16，整条 episode 逐控制步吃数据集真实观测，模型动作 vs 数据集动作逐帧对比+出图 /tmp/tf_rollout_ep{N}.png，系统 python3 matplotlib 画图）。**任务语义（用户纠正后钉死）**：task0=pick up（起始双爪空 0% → 预抓取张开 L@0.11·n / R@0.56·n → 抓住后指令 0%=收紧、state 被物体撑在 ~33.5%）；task1=places（起始双爪持物 state 33%=被撑、指令 0%=持续收紧 → L@0.27·n 释放 / R@0.66·n 张开 100% → 末态空爪 0%）。**夹爪语义：action=指令宽度%，state=物理宽度%（持物时被物体撑开，≠指令）**——两条 stream 对不上是物理不是 bug。
- **ep0（task0）回放全绿**：臂 mean err 0.014 rad（p95 0.063）、delta cos p50 0.92、**开爪时机同帧（339 vs 339）**、持物保持 0% ✓、推理 p50 345ms（bf16 稳态）。
- **task1（ep120/ep150）臂好爪坏**：臂全程跟踪（abs err ~0.019 rad）但夹爪通道错——持物段乱张 40-80%（两次开爪相位一致 0.128·n，非随机）、释放段该 100% 只输出 ~0。
- **指令交换探针（同观测换 places/pick up 指令）**：夹爪输出几乎不变（帧96 两条指令都张 66-69%，帧380 该 100% 都输出 ~0）→ **语言接地对夹爪无效**，task0 的预抓取本能泄漏进 task1（疑似视觉相似段触发 task0 时刻表）。**结论：task1 夹爪时序未学会=训练侧缺陷**（部署无解），方向：加长训练/检查 task1 夹爪标签/强化语言条件。
- **部署建议**：真机先跑 task0（pick up 原句 + episode_start_task0.json 预热 + bf16 + delta 执行链）；task1 待训练修复后 tf_matrix/tf_rollout 复验再上。

**【2026-09-24·真机影子回放 ep0 全绿】执行链闭环**：`run_g1_replay.py` 轨迹锁定版全程 198/198 控制步，跟踪滞后 p50 20 / max 59 mrad（vs 首跑时间锁的 1 rad 平台+剧烈抖动），门等待 p50 0.1s，总时长 235s（慢放~12×），峰值行程 2611 mrad 护栏内，夹爪开/闭时序与数据集同帧、终态双爪闭合 ✅。速度定标：每控制步位移 p95 0.147 rad ÷ 0.75s 窗口 → `[replay].speed`=0.15。详见 [[galbot-g1-pvt-trajectory-hazard]]（轨迹锁定原理）与 BENCHMARKS 附录。部署链"bf16 权重+delta 换算+数据集观测装配"真机复现采集轨迹成立；下一步才轮到真观测闭环（run_g1_loop）与 task1 修复复验。

